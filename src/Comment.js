import React  from 'react';

const Comment = (props, context) => (
  <div>
    <span style={{}}>{props.name}</span>
  </div>
);
export {Comment as default}
/*  <button onClick={hideComent}>hide</button>*/
// /*  <span /*onClick={props.onClick}*/>{props.index}</span> */
